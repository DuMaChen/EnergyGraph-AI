# CASE-001 课程知识问答

问题：请解释抽水蓄能电站的组成和工作原理，并说明它与电化学储能在应用场景上的主要区别。

- 标准关键点：上水库/下水库、输水系统、可逆式机组；抽水与发电过程；两类储能应用差异。
- 必须核验：来源文件、章节、页码、manifest 版本。
- 本地结果：`PYTHONPATH=/tmp/jbgs-agent-deps python3 scripts/test_adapter_runtime.py` 通过；Mock SSE 包含 `token -> source -> done`，来源文件与课程 manifest 一致。
- 真实结果：待讯飞凭证、真实 Workflow 发布版本和专业教师核验后回填。
- 教师评分：待人工验收。
- request ID / sid：本地夹具每次生成临时 request ID；真实 sid 待讯飞调用。
- 结论：本地协议通过；正式竞赛案例为“待真实环境复测”。

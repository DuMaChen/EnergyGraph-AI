# 电力系统储能技术课程 Agent 平台 · 项目现状

> 状态快照：2026-09-09（Asia/Shanghai）
> 仓库：[DuMaChen/EnergyGraph-AI](https://github.com/DuMaChen/EnergyGraph-AI)（服务器本地 git 仓库已初始化，main 分支）
> 历史档案：[DEVELOPMENT_ARCHIVE.md](./DEVELOPMENT_ARCHIVE.md) · 旧状态快照（2026-08-22）：[archive/PROJECT_STATUS_20260822.md](./archive/PROJECT_STATUS_20260822.md)
> 本轮维护详情：[docs/MAINTENANCE_2026-09.md](./docs/MAINTENANCE_2026-09.md) · 运维手册：[docs/OPERATIONS.md](./docs/OPERATIONS.md)

## 1. 一句话结论

平台已上线生产（HTTPS：`https://energygraph.icu/agent/`），三角色教学业务闭环可用并经过多轮实机验证；2026-09-07 至 09-09 完成对话 Agent 可靠性专项维护——**清除全部 8 处保底伪造逻辑、修复成绩册回写三处断点、建立工作流劫持识别-复位-重试与出题缓冲验证机制**，并按"师生各 7 轮 × 4 类功能"完成推送前后压测。

## 2. 部署与运行

| 项目 | 当前记录 |
|---|---|
| 生产地址 | `https://energygraph.icu`（工作空间 `/agent/`） |
| 服务器 | 阿里云，Docker Compose：Caddy + Moodle 4.5 + Agent UI + Agent Adapter + MariaDB |
| 容器状态 | 全部 running / healthy |
| 部署目录 | `/opt/jbgs-course-agent`（git 仓库，main） |
| 账号 | teacher / student / admin（详见 USER_MANUAL.md） |

## 3. 功能现状矩阵

| 功能 | 状态 | 验证方式 |
|---|---|---|
| 正常知识问答（含图谱考点直达、划词追问） | 部分受限* | 链路正常；问候语问题待工作流控制台修正 |
| 随堂单题（出题/判分/批改讲解/停止） | 可用 | 实机全链通过 |
| 学情诊断（出题/判分/报告/退出/补题自愈） | 可用 | 实机全链通过，报告真实生成 |
| 情景演绎（师傅/名师，开启/对答/退出） | 可用 | 实机全链通过，退出同步清工作流状态 |
| 教师 AI 备课 / AI 出卷 | 可用 | 出卷带标准答案与解析（严格重试保障） |
| 作业发布 / 提交 / 批改 | 可用 | 实机全链通过 |
| 成绩同步教务成绩册 | 可用 | 批改→synced→成绩册落库→学生可见，全链实测 |
| 班级学情看板 / 单生查询 | 可用 | 指标透明公式 |
| 思维导图（浏览/编辑/云同步/左侧悬浮缩放/滚轮缩放） | 可用 | 页面级验证 + 数学模拟 |
| 管理员配置（AI 规则读写/重置）与权限边界 | 可用 | 学生访问管理接口 403 实测 |

\* 普通问答当前回问候语：工作流"知识问答模型"的 `input` 参数被误绑为知识库检索结果（应为开始节点 `AGENT_USER_INPUT`），需在讯飞控制台改回，见维护日志第 7 节。

## 4. 可靠性机制（本轮新增）

- **零保底伪造**：出题/判分/报告/情景开场共 8 处欺骗性兜底全部清除，失败一律如实提示；
- **出题缓冲验证**：四条出题路径先缓冲验格（题干+选项+答案字母+去重+告别语识别）再展示，不合格自动重试一次；
- **答案探针**：工作流丢失答案字段时，经批改分支取回其持久化的真实答案；
- **集中错误日志**：所有上游失败以 `[WORKFLOW_ERROR]` 落日志并带错误码下发；
- 细节与根因见 [docs/MAINTENANCE_2026-09.md](./docs/MAINTENANCE_2026-09.md)。

## 5. 课程数据基线（不变）

20 份课件 PDF / 439 页切片（文本覆盖 100%）/ 6 章 / 20 个知识点 / 17 条关系；基线文件 `course-data/normalized/manifest.json` 与 `graph-baseline.json`。

## 6. 质量与发布纪律

- 单元测试 19/19（诚实兜底、诊断状态机、工作流状态机三套件）；
- 推送前必须完成多轮实机验证（师生各约 7 轮 × 问答/单题/诊断/情景），推送后再做回归——详见 [docs/OPERATIONS.md](./docs/OPERATIONS.md) 第 5 节；
- 教训记录：UI 改动必须预先评估对既有机制的影响，思维导图缩放一次返工即源于此（95f2da2 → c588635 回滚 → b1094ff 修正）。

## 7. 待办

1. **（控制台）** 知识问答模型 `input` 改回 `AGENT_USER_INPUT`——普通问答恢复的前提；
2. **（控制台）** 变量存储器_6 变量作用域、`ansewr9` 拼写、知识库_6 文件数核查；
3. **（平台）** "通知与讨论"板块目前为前端写死演示数据，需补通知 CRUD（后端表+接口+前端接线）；
4. **（平台）** 出题请求可考虑携带对话历史，进一步提升跨题防重命中率。

## 8. 不变承诺

- 零表情符号工程红线；
- 讯飞密钥仅存 `deploy/.env`，不入前端/日志/仓库；
- 每日 cron 备份与恢复演练机制照旧（最近演练点见历史档案）。
